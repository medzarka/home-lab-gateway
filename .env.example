# ==============================================================================
# Homelab Gateway Ingress & Arcane Configuration
# ==============================================================================

# --- 1. Base Storage Directory ---
GATEWAY_HOME=/srv/data/gateway

# --- 2. Cloudflare ACME DNS Challenge (Wildcard SSL) ---
# Cloudflare API Token with "Zone:DNS:Edit" and "Zone:Zone:Read" permissions
CLOUDFLARE_API_TOKEN=your_cloudflare_dns_api_token_here

# Email registered for Let's Encrypt renewal notifications
ACME_EMAIL=your_email@domain.com

# Domain configuration
ROOT_DOMAIN=bluewave.work
ARCANE_HOSTNAME=arcane.bluewave.work
DASHBOARD_HOSTNAME=homelab.bluewave.work

# --- 3. Arcane Security Keys (Generate with `openssl rand -base64 32`) ---
ARCANE_ENCRYPTION_KEY=generate_with_openssl_rand_base64_32
ARCANE_JWT_SECRET=generate_with_openssl_rand_base64_32
ARCANE_PUID=1000
ARCANE_PGID=1000

# --- 4. Traefik Dashboard Security (Optional BasicAuth) ---
# Generate hash with `htpasswd -nb admin your_password`
TRAEFIK_BASIC_AUTH_USER=admin
TRAEFIK_BASIC_AUTH_HASH=$$apr1$$...

# --- 5. Cluster Interconnect Tailscale IPs ---
# Internal Tailscale IPs for cross-VM mesh proxying
TS_VM1_ZAP_VPS=100.119.246.120
TS_VM2_OCI01=100.83.191.68
TS_VM3_ORANGEPI=100.71.154.78

# --- 6. Timezone ---
TZ=UTC
